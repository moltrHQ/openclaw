# OpenClaw Docker Configuration
# Copy this file to .env and fill in the values:
#   cp .env.example .env

# Provider: minimax, openai, or deepseek
PROVIDER_KEY=minimax

# Your API key from the provider
# Minimax: Create an account at https://www.minimax.io
# OpenAI: https://platform.openai.com/api-keys
# DeepSeek: https://platform.deepseek.com
PROVIDER_API_KEY=your-api-key-here

# Gateway token (leave empty = auto-generated)
GATEWAY_TOKEN=

# ─── Optional: Moltr Security Shield ───────────────────────────────────────
# Uncomment the moltr service in docker-compose.yml to enable it.
# Generate keys: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

MOLTR_API_KEY=your-moltr-api-key-here
MOLTR_SCAN_PASSPHRASE=change-me
MOLTR_FERNET_KEY=your-fernet-key-here
MOLTR_KILLSWITCH_CODEPHRASE=change-me
MOLTR_WATCHDOG_HMAC_KEY=your-hmac-key-here
MOLTR_DASHBOARD_USER=admin
MOLTR_DASHBOARD_PASS_HASH=
MOLTR_JWT_SECRET=your-jwt-secret-here

# Cookie SameSite policy: lax (default) | strict | none
MOLTR_COOKIE_SAMESITE=lax

# Trusted reverse proxy IP/CIDR — set to 127.0.0.1 when running behind Caddy/nginx on the same host.
# Without this, X-Forwarded-For is ignored (spoofing protection).
MOLTR_TRUSTED_PROXY=127.0.0.1
